spring:
  application:
    name: registry-server
server:
  port: 8761


eureka:
  client:
    register-with-eureka: false # No se registra a sí mismo en Eureka
    fetch-registry: false
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
